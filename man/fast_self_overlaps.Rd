% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derived_overlaps.R
\name{fast_self_overlaps}
\alias{fast_self_overlaps}
\title{Self Overlaps}
\usage{
fast_self_overlaps(
  x,
  max_gap = -1L,
  min_overlap = 0L,
  type = c("any", "start", "end", "within", "equal"),
  ignore_strand = FALSE,
  threads = fast_default_threads(),
  deterministic = TRUE,
  drop_self = TRUE,
  drop_redundant = TRUE
)
}
\arguments{
\item{x}{An \code{IRanges} or \code{GRanges} object.}

\item{max_gap}{Integer scalar maximum allowed gap. A value of \code{-1}
requires direct overlap.}

\item{min_overlap}{Integer scalar minimum overlap width.}

\item{type}{Overlap mode: one of \code{"any"}, \code{"start"}, \code{"end"},
\code{"within"}, or \code{"equal"}.}

\item{ignore_strand}{Logical scalar. Ignored for non-genomic ranges.}

\item{threads}{Integer scalar thread count.}

\item{deterministic}{Logical scalar. If \code{TRUE}, output is sorted by
\verb{(query_id, subject_id)}.}

\item{drop_self}{Logical scalar. If \code{TRUE}, self-hits (\code{i} vs \code{i}) are
removed.}

\item{drop_redundant}{Logical scalar. If \code{TRUE}, redundant pairs are removed
for self-comparisons by keeping only \code{query_id < subject_id}.}
}
\value{
A \code{S4Vectors::Hits} object.
}
\description{
Find overlaps within a single range object.
}
\examples{
x <- IRanges::IRanges(start = c(1, 3, 10), end = c(5, 8, 12))
fast_self_overlaps(x)
}
