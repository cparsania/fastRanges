% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nearest.R
\name{fast_follow}
\alias{fast_follow}
\title{Follow Query Ranges}
\usage{
fast_follow(
  query,
  subject,
  ignore_strand = FALSE,
  threads = fast_default_threads()
)
}
\arguments{
\item{query}{An \code{IRanges} or \code{GRanges} query object.}

\item{subject}{An \code{IRanges} or \code{GRanges} subject object.}

\item{ignore_strand}{Logical scalar. Ignored for non-genomic ranges.}

\item{threads}{Integer scalar thread count. Included for API consistency.}
}
\value{
Integer vector of subject indices, with \code{NA} for unmatched queries.
}
\description{
Return the index of the first subject range that is strictly before each
query range.
}
\details{
For each query range, return the index of the first subject range that comes
strictly before the query.
}
\examples{
q <- IRanges::IRanges(start = c(1, 10, 20), width = c(5, 4, 3))
s <- IRanges::IRanges(start = c(3, 9, 18), width = c(4, 6, 5))
fast_follow(q, s)
}
