% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterator.R
\name{fast_iter_next}
\alias{fast_iter_next}
\title{Advance an Overlap Iterator}
\usage{
fast_iter_next(iter)
}
\arguments{
\item{iter}{A \code{fast_ranges_iter} object.}
}
\value{
A \code{S4Vectors::Hits} object for the next chunk, with global query
indices.
}
\description{
Compute overlaps for the next query chunk.
}
\details{
Each call advances the iterator state.

The returned \code{Hits} object uses global query indices, not chunk-local
indices, so you can combine chunk outputs safely.
}
\examples{
q <- IRanges::IRanges(start = c(1, 10, 20), width = c(5, 4, 3))
s <- IRanges::IRanges(start = c(3, 9, 18), width = c(4, 6, 5))
it <- fast_find_overlaps_iter(q, s, chunk_size = 2L)
fast_iter_next(it)
}
