% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterator.R
\name{fast_find_overlaps_iter}
\alias{fast_find_overlaps_iter}
\title{Create an Overlap Iterator}
\usage{
fast_find_overlaps_iter(
  query,
  subject,
  chunk_size = 50000L,
  max_gap = -1L,
  min_overlap = 0L,
  type = c("any", "start", "end", "within", "equal"),
  ignore_strand = FALSE,
  threads = fast_default_threads(),
  deterministic = TRUE
)
}
\arguments{
\item{query}{An \code{IRanges} or \code{GRanges} query object.}

\item{subject}{An \code{IRanges}/\code{GRanges} object or a \code{fast_ranges_index}.}

\item{chunk_size}{Integer scalar number of query ranges per chunk.}

\item{max_gap}{Integer scalar maximum allowed gap. A value of \code{-1}
requires direct overlap.}

\item{min_overlap}{Integer scalar minimum overlap width.}

\item{type}{Overlap mode: one of \code{"any"}, \code{"start"}, \code{"end"},
\code{"within"}, or \code{"equal"}.}

\item{ignore_strand}{Logical scalar. Ignored for non-genomic ranges.}

\item{threads}{Integer scalar thread count.}

\item{deterministic}{Logical scalar. If \code{TRUE}, output is sorted by
\verb{(query_id, subject_id)}.}
}
\value{
A \code{fast_ranges_iter} iterator object.
}
\description{
Create an iterator that computes overlaps in query chunks.
}
\examples{
q <- IRanges::IRanges(start = c(1, 10, 20), width = c(5, 4, 3))
s <- IRanges::IRanges(start = c(3, 9, 18), width = c(4, 6, 5))
it <- fast_find_overlaps_iter(q, s, chunk_size = 2L)
fast_iter_has_next(it)
}
