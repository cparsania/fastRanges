name: pkgdown

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pkgdown-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::BiocManager
            any::pkgdown
            any::roxygen2
            bioc::BiocStyle
            bioc::XVector
            local::.

      - name: Regenerate Rd documentation
        run: |
          Rscript -e 'roxygen2::roxygenise(".")'

      - name: Verify overlap argument docs stay in sync
        run: |
          Rscript tools/check_overlap_doc_inheritance.R

      - name: Build site
        run: |
          Rscript -e 'options(repos = BiocManager::repositories()); BiocManager::install("XVector", ask = FALSE, update = FALSE); pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)'

      - name: Verify pkgdown output
        run: |
          test -f docs/index.html
          test -f docs/reference/index.html
          test -f docs/reference/fast_anti_overlap_join.html
          grep -q "fast_anti_overlap_join" docs/reference/index.html
          grep -q "Overlap semantics" docs/reference/fast_anti_overlap_join.html
          echo "pkgdown index found at docs/index.html"
          ls -la docs | sed -n '1,120p'
          ls -la docs/reference | sed -n '1,120p'

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          publish_branch: gh-pages
          force_orphan: true
          enable_jekyll: false
